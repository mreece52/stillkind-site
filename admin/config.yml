backend:
  name: github
  repo: Mreece52/stillkind-site
  branch: main

# Where uploaded images (if any) get stored
media_folder: "content/uploads"
public_folder: "/content/uploads"

collections:
  - name: "events"
    label: "Events"
    files:
      - name: "events"
        label: "Events List"
        file: "content/events/events.json"
        icon: "event"
        fields:
          - name: "events"
            label: "Events"
            label_singular: "Event"
            widget: "list"
            root: true
            summary: "{{fields.title}} — {{fields.date}}"
            fields:
              - label: "Event Name"
                name: "title"
                widget: "string"
                hint: "e.g. Pour It Forward"
              - label: "Date"
                name: "date"
                widget: "datetime"
                date_format: "YYYY-MM-DD"
                time_format: false
                hint: "The day the event happens"
              - label: "Location"
                name: "location"
                widget: "string"
                hint: "e.g. Primetime Bar & Grill"
              - label: "Cause / Description"
                name: "cause"
                widget: "string"
                required: false
                hint: "e.g. Benefiting The Friendship Center"

  - name: "merch"
    label: "Merch"
    files:
      - name: "merch"
        label: "Merch Items"
        file: "content/merch/merch.json"
        icon: "shopping_bag"
        fields:
          - name: "merch"
            label: "Items"
            label_singular: "Item"
            widget: "list"
            root: true
            summary: "{{fields.title}}"
            fields:
              - label: "Label"
                name: "title"
                widget: "string"
                hint: "Small label above the headline, e.g. Still Kind Merch"
              - label: "Headline"
                name: "headline"
                widget: "string"
                hint: "e.g. Wear the movement."
              - label: "Description"
                name: "description"
                widget: "text"
              - label: "Link"
                name: "link"
                widget: "string"
                hint: "Where the button goes — a URL, or #contact to link to the contact form"
              - label: "Button Text"
                name: "link_text"
                widget: "string"
                hint: "e.g. Shop Merch"
